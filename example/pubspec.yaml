name: flutter_swipe_to_reply_example
description: Demonstrates swipe-to-reply interactions in a Flutter conversation.
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_swipe_to_reply:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
