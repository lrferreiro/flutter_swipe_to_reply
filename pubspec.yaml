name: flutter_swipe_to_reply
description: A lightweight Flutter swipe-to-reply wrapper for chat bubbles, list rows, cards, and any custom widget.
version: 0.2.0
homepage: https://pub.dev/packages/flutter_swipe_to_reply
repository: https://github.com/lrferreiro/flutter_swipe_to_reply
issue_tracker: https://github.com/lrferreiro/flutter_swipe_to_reply/issues
topics:
  - chat
  - messaging
  - gestures
  - swipe
  - widgets

environment:
  sdk: ^3.12.2
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
